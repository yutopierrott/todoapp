# はじめてのClaude Code入門ガイド

このドキュメントでは、Claude Codeを使ってWebアプリを開発し、GitHubにアップロードして、Vercelで公開するまでの流れを、初心者向けにやさしく解説します。

## 目次

1. [Claude Codeとは何か？](#1-claude-codeとは何か)
2. [ClaudeアプリのCodeタブでウェブアプリを開発する方法](#2-claudeアプリのcodeタブでウェブアプリを開発する方法)
3. [GitHubへのコードのアップロード方法](#3-githubへのコードのアップロード方法)
4. [Vercelでインターネット公開する方法](#4-vercelでインターネット公開する方法)
5. [エラーが出たときのAIへの聞き方](#5-エラーが出たときのaiへの聞き方)

---

## 1. Claude Codeとは何か？

**Claude Code**は、Anthropic社が提供するAI搭載の開発支援ツールです。一言でいうと「コードを読んだり書いたり、実際にコマンドを実行したりできるAIアシスタント」です。

普通のチャットAIは「コードの書き方を教えてくれる」だけですが、Claude Codeは違います。

- あなたのプロジェクトのファイルを実際に**読んで理解**する
- 新しいファイルを**作成**したり、既存のファイルを**編集**したりする
- ターミナルコマンドを**実行**する（テストを走らせる、パッケージをインストールするなど）
- Gitでの変更管理やGitHubへの操作も行える

つまり「指示を出すと、実際に手を動かしてプロジェクトを完成させてくれるエンジニア」のようなイメージです。

### 利用できる環境

Claude Codeにはいくつかの使い方があります。

| 環境 | 特徴 |
|---|---|
| ターミナル（CLI） | 自分のPC上でコマンドとして起動して使う |
| デスクトップアプリ（Mac/Windows） | GUIアプリとして使う |
| **Web版（claude.aiのCodeタブ）** | ブラウザだけで使える。インストール不要 |
| IDE拡張機能（VS Code、JetBrainsなど） | 普段使っているエディタの中で使う |

初心者の方には、インストール作業が不要な**Web版（Codeタブ）**が特におすすめです。次の章で詳しく説明します。

---

## 2. ClaudeアプリのCodeタブでウェブアプリを開発する方法

Claude Codeの中でも、`claude.ai`にログインするだけで使えるのが「Codeタブ」です。ブラウザさえあれば、自分のPCに何もインストールせずに開発を始められます。

### 基本的な流れ

1. **claude.aiにログイン**し、画面上部（またはサイドバー）にある「Code」タブを開きます。
2. **セッションを作成**します。ゼロから新しいプロジェクトを始める場合も、既存のGitHubリポジトリを指定して作業する場合も選べます。
3. **やりたいことを日本語で入力**します。例えば以下のように具体的に書くと精度が上がります。

   > 「タスクを追加・削除・完了にできるTodoアプリを作りたいです。フロントエンドはReact、見た目はシンプルでモダンにしてください。」

4. Claudeが**実際にファイルを作成・編集**し、必要なコマンド（依存パッケージのインストールなど）を実行しながらアプリを組み立てていきます。作業内容は画面に逐次表示されます。
5. できあがったら、**動作確認**をお願いしましょう。「ブラウザで実際に動かして確認して」と頼むと、Claude自身がアプリを起動してチェックしてくれます。
6. 気になる点があれば、追加で指示を出して**修正・改善**を繰り返します。会話のキャッチボールで少しずつ完成度を上げていくイメージです。

### 初心者向けのコツ

- **一度に完璧を求めない**：まず最小限動くものを作ってもらい、そこから機能を追加していく方がスムーズです。
- **具体的に伝える**：「いい感じにして」より「ボタンの色を青にして」「入力欄を上に移動して」のように具体的に伝えましょう。
- **わからない言葉は聞く**：「〇〇って何ですか？」とそのまま質問してOKです。Claudeが説明してくれます。

---

## 3. GitHubへのコードのアップロード方法

作ったアプリはGitHubというサービスにアップロード（＝プッシュ）して保存・管理するのが一般的です。GitHubに置いておくと、変更履歴が残ったり、後述のVercelと連携して公開したりできるようになります。

### 事前準備：GitHubアカウントとの連携

1. GitHubアカウントを持っていない場合は、[github.com](https://github.com)で無料アカウントを作成します。
2. claude.aiの**設定（Settings）→ Connectors（連携）**からGitHubを接続し、Claude Codeがあなたの代わりに操作できるように認可（Authorize）します。
3. **プライベートリポジトリ**を使う場合は、そのリポジトリに対して「Claude」のGitHub Appをインストール（許可）しておく必要があります。パブリックリポジトリであれば、この手順は基本的に不要です。
4. 会社などの**組織アカウント（Team/Enterprise）**で利用している場合は、管理者が先に管理画面で「GitHub連携」を有効化しておく必要があります。個人アカウント（Pro/Max）であれば、初回ログイン時の案内に沿って連携するだけでOKです。

### アップロードの流れ

Codeタブでの作業中に、そのまま日本語で次のようにお願いするだけです。

> 「このコードをGitHubにアップロードしてください」

これだけで、Claudeが裏側で以下のような作業を行います。

1. 変更内容を**コミット**（変更の記録）としてまとめる
2. 指定した**ブランチにプッシュ**（GitHub上へ送信）する
3. 必要であれば**プルリクエスト（PR）**を作成する（複数人での確認・レビューを経てから本番に取り込む仕組み）

Claude Codeは基本的に、明示的に「プルリクエストを作って」と頼んだり、画面上の「Create PR」ボタンを押したりしない限り、勝手にPRを作ることはありません。まずは「アップロードして」「変更を保存して」のような自然な言葉で伝えれば十分です。

### 確認方法

アップロードが終わったら、GitHubのリポジトリページ（`https://github.com/ユーザー名/リポジトリ名`）を開いて、ファイルが反映されているか確認しましょう。Claudeに「アップロードできたか確認して」と聞くのも良い方法です。

---

## 4. Vercelでインターネット公開する方法

GitHubにコードを置いたら、次は**Vercel**を使って世界中の誰でもアクセスできる形で公開（デプロイ）してみましょう。VercelはWebアプリのホスティングサービスで、GitHubと連携させるだけで自動的に公開・更新ができる点が初心者にも扱いやすいです。

### 手順

1. [vercel.com](https://vercel.com)にアクセスし、**「Continue with GitHub」**などのボタンでGitHubアカウントを使ってサインアップします。
2. ダッシュボードの**「Add New...」→「Project」**を選択します。
3. 先ほどアップロードした**GitHubリポジトリを選んで「Import」**します。
4. フレームワーク（React、Next.jsなど）は多くの場合Vercelが自動検出してくれます。特別な設定が不要であれば、そのまま**「Deploy」**ボタンを押します。
5. 数十秒〜数分待つと、`https://プロジェクト名.vercel.app`のようなURLが発行され、そこにアクセスすればアプリが動いています。

### 公開後の運用

一度GitHub連携でVercelにデプロイすると、その後は**GitHubに新しい変更をプッシュするたびに自動的に再デプロイ**されます。つまり、Claude Codeで修正 → GitHubにアップロード、という流れを繰り返すだけで、公開中のアプリも自動的に最新版に更新されていきます。

### つまずきやすいポイント

- ビルドが失敗する場合、Vercelのダッシュボードに表示される**ビルドログ（エラーメッセージ）**を確認しましょう。そのログをコピーしてClaudeに貼り付け、「Vercelのデプロイでこのエラーが出ました、直してください」と伝えると解決が早いです。
- 環境変数（APIキーなど）が必要なアプリの場合は、Vercelのプロジェクト設定にある**「Environment Variables」**にも同じ値を登録する必要があります。

---

## 5. エラーが出たときのAIへの聞き方

開発をしていると、エラーは必ずと言っていいほど発生します。焦らず、Claudeに正しく状況を伝えることが解決への近道です。

### 良い聞き方のポイント

1. **エラーメッセージは全文をそのまま貼り付ける**
   要約せず、赤字で表示されたエラー文やターミナルの出力をコピー＆ペーストしましょう。一部を省略すると原因特定が難しくなります。

   > 悪い例：「なんかエラーが出ました」
   > 良い例：「以下のエラーが出ました。`TypeError: Cannot read properties of undefined (reading 'map')` at src/App.jsx:23」

2. **何をしようとしていたか（再現手順）を伝える**
   「〇〇のボタンを押したら」「npm run devを実行したら」のように、エラーが発生するまでの操作を具体的に書きます。

3. **期待していた動作を伝える**
   「本来はタスク一覧が表示されるはずだった」など、正しい状態も伝えると、Claudeが差分を理解しやすくなります。

4. **すでに試したことがあれば伝える**
   「パッケージを再インストールしてみたが直らなかった」などの情報があれば、無駄な提案を避けられます。

### Claude Codeならではの強み

Claude Code（特にCodeタブ）では、エラーメッセージを貼り付けなくても、

> 「さっき実行したらエラーになりました。原因を調べて直してください」

と伝えるだけで、Claude自身がログやファイルを確認し、原因を特定してくれることも多いです。人間同士のデバッグと同じように、遠慮せず「動かない」「おかしい」とそのまま伝えて構いません。

### 困ったときは

- 操作方法自体がわからない場合は `/help` と入力すると、Claude Codeの使い方に関するヘルプを確認できます。
- それでも解決しない不具合や、Claude Codeというツール自体の問題だと感じた場合は、[https://github.com/anthropics/claude-code/issues](https://github.com/anthropics/claude-code/issues) から報告できます。

---

以上が、Claude Codeを使ってアイデアを形にし、世界に公開するまでの基本的な流れです。まずは小さなアプリから、実際に手を動かして試してみましょう。
